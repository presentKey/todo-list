import styles from './TodoList.module.css';

function TodoList() {
  return (
    <div className={styles.todoList}>
      <section className={styles.todoListSection}></section>
    </div>
  );
}

export default TodoList;
